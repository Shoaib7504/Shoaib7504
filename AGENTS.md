# AGENTS.md — Shoaib7504/Shoaib7504

**GitHub Profile README** (not a code project). Single `main` branch, no tags.

## What this repo is

A single `README.md` rendered on https://github.com/Shoaib7504 plus three decorative SVGs.  
There is no source code, no package manager, no config, no CI, no tests, no formatter, no typechecker.

## Editing workflow

- Edit `README.md` directly — there are no build or preview steps.
- SVG filenames contain spaces and parentheses, e.g. `banner (2).svg`. Quote paths in shell commands.
- External image/service URLs in the README (`img.shields.io`, `github-readme-stats`, `streak-stats`, `github-readme-activity-graph`, `github-profile-trophy`, `readme-typing-svg.demolab.com`, `profile-counter.glitch.me`) are live dependencies. Verify they still resolve after changes.

## Commands

None. No `npm`, `pip`, `make`, or any other toolchain is used.
